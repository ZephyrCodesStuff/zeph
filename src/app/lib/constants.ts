import { Vector3 } from "three";

export const BASE_CAMERA_POSITION = new Vector3(-1.75, -0.5, 3);
export const BASE_LOOK_AT = new Vector3(-1.35, -0.2, 0);
export const BASE_ZOOM = 1.0;
export const MOBILE_ZOOM = 0.7; // Less zoom for mobile 